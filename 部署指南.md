# 酷态科10号充电器 BLE 控制服务 - 部署指南

## 一、项目简介

本项目通过蓝牙 BLE 控制酷态科10号充电器（AD1204U），实现端口开关、功率监控、充电时长限制、倒计时、定时任务、智能充电保护等功能。提供 Web 管理界面，支持 PC 浏览器访问。

- **服务地址**：http://127.0.0.1:8080/
- **配置页面**：http://127.0.0.1:8080/config.html
- **技术栈**：Python 3.10+ / aiohttp / bleak / paho-mqtt / PyYAML

---

## 二、环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（仅支持 Windows，BLE 栈依赖 Windows） |
| Python | 3.10 及以上（推荐 3.11/3.12） |
| 蓝牙 | 电脑需带蓝牙 4.0+ 适配器 |
| 网络 | 首次安装需联网下载依赖包 |
| 浏览器 | Edge / Chrome / Firefox（仅 PC 使用） |

### 安装 Python

1. 访问 https://www.python.org/downloads/ 下载 Python 3.11+
2. 安装时**务必勾选** "Add Python to PATH"
3. 安装完成后，打开命令行输入 `python --version` 验证

---

## 三、快速开始（一键安装）

1. 将本项目文件夹解压到任意位置（建议路径不含中文和空格，如 `D:\cuktech-ble-server`）
2. 双击运行 `install.bat`
3. 等待安装完成（自动创建虚拟环境、安装依赖、配置自启动、创建桌面快捷方式）
4. 编辑 `config.yaml`，填入你的 BLE 和巴法云配置（详见下文）
5. 双击桌面 "酷态科充电控制" 快捷方式启动服务

---

## 四、手动安装步骤

如果一键安装失败，可手动执行以下步骤：

### 4.1 创建虚拟环境

```bash
cd 项目文件夹
python -m venv venv
```

### 4.2 安装依赖

```bash
venv\Scripts\python.exe -m pip install --upgrade pip
venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 4.3 配置开机自启动

```powershell
# 以管理员或普通用户身份运行（当前用户自启动）
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "CuktechChargerAutoStart" /t REG_SZ /d "wscript.exe \"项目完整路径\start_service.vbs\"" /f
```

### 4.4 创建桌面快捷方式

手动创建快捷方式，目标设为：
```
wscript.exe "项目完整路径\quick_start.vbs"
```

---

## 五、配置说明（config.yaml）

编辑项目根目录下的 `config.yaml`，以下是各字段说明：

### 5.1 BLE 配置（必须）

```yaml
ble:
  mac: "XX:XX:XX:XX:XX:XX"      # 充电器蓝牙 MAC 地址
  token: "00000000000000000000000000000000"  # 设备认证 Token（十六进制）
  ble_key: "00000000000000000000000000000000"  # 加密密钥（十六进制）
  scan_timeout: 15
```

**这三个值（mac / token / ble_key）必须通过小米云获取，获取方法见第六节。**

### 5.2 巴法云配置（可选，用于远程控制）

```yaml
bemfa:
  enabled: true
  uid: "你的巴法云私钥"  # 巴法云用户私钥，获取方法见第七节
  name_c1: "酷态科C口1"   # C1 端口在巴法云的显示名
  name_c2: "酷态科C口2"
  name_c3: "酷态科C口3"
  name_a: "酷态科USB-A"
  name_ble: "酷态科蓝牙"
```

### 5.3 服务器配置

```yaml
server:
  port: 8080                    # Web 服务端口
  host: 0.0.0.0                 # 监听地址（0.0.0.0 允许局域网访问）
  idle_shutdown_seconds: 0      # 空闲自动退出秒数，0=常驻后台（推荐 0）
  history_retention_days: 365   # 充电历史保留天数
  log_level: info                # 日志级别
```

> **重要**：`idle_shutdown_seconds: 0` 表示服务常驻后台，不会因浏览器关闭而退出。如需关闭浏览器后自动退出，可设为 30（秒）。

### 5.4 服务管理

```yaml
service:
  auto_start: true                    # 开机自启动（需配合注册表项）
  daily_restart_enabled: true         # 每日定时重启
  daily_restart_time: "06:30"         # 每日重启时间
```

### 5.5 充电保护

```yaml
protection:
  enabled: true
  low_power_w: 15              # 低功率触发阈值（W）
  low_power_minutes: 30        # 低功率持续时长（分钟）
```

### 5.6 端口最大充电时长

```yaml
charge_limit:
  c1: 120    # C1 端口最大充电分钟数，0=不限制
  c2: 120
  c3: 120
  a: 120
```

---

## 六、小米云获取 BLE 信息（MAC / Token / BLE Key）

酷态科充电器的蓝牙认证信息（MAC、Token、BLE Key）需要通过小米云获取。本项目内置了小米云自动获取功能，也可手动获取。

### 方法一：通过配置页自动获取（推荐）

1. 启动服务后，打开 http://127.0.0.1:8080/config.html
2. 在 "BLE 设备" 卡片右上角，点击 "小米云自动获取" 链接
3. 在弹出的小米云登录窗口中，登录你的小米账号（需已绑定酷态科充电器）
4. 登录成功后，系统会自动获取并填入 MAC、Token、BLE Key
5. 点击 "保存配置并重启"

### 方法二：手动获取

如果自动获取失败，可通过以下方式手动获取：

1. **确保充电器已通过米家 App 配网并绑定到你的小米账号**
2. 使用抓包工具（如 Charles、Fiddler）抓取米家 App 与充电器的蓝牙通信数据
3. 在蓝牙 GATT 通信中找到：
   - **MAC 地址**：充电器蓝牙广播的 MAC（格式如 `XX:XX:XX:XX:XX:XX`）
   - **Token**：设备认证 Token（16 进制字符串）
   - **BLE Key**：加密密钥（16 进制字符串）
4. 将获取的值填入 `config.yaml` 的 `ble` 部分

> **注意**：Token 和 BLE Key 是敏感信息，请勿泄露给他人。每个充电器的 Token/Key 是唯一的。

---

## 七、巴法云私钥获取教程

巴法云（bemfa.com）用于实现远程控制充电器（通过巴法云 MQTT 转发）。

### 7.1 注册巴法云账号

1. 访问 https://cloud.bemfa.com/
2. 点击 "注册"，用手机号注册账号并登录

### 7.2 获取私钥

1. 登录后，点击右上角头像或 "控制台"
2. 在 "个人信息" 或 "账户设置" 页面，找到 "私钥" 或 "UID"
3. 复制你的私钥（32 位十六进制字符串，格式如 `00000000000000000000000000000000`）
4. 将私钥填入 `config.yaml` 的 `bemfa.uid` 字段

### 7.3 配置端口名称

在巴法云控制台的 "TCP 设备" 或 "MQTT 设备" 页面，可添加设备并设置名称。端口名称需与 `config.yaml` 中的 `name_c1/name_c2/name_c3/name_a/name_ble` 对应。

> 巴法云免费版有设备数量限制，通常足够 5 个设备（4 端口 + 蓝牙状态）使用。

---

## 八、启动方式

### 方式一：桌面快捷方式（推荐）

双击桌面 "酷态科充电控制" 快捷方式，自动检测服务状态并打开浏览器。

### 方式二：命令行启动

```bash
cd 项目文件夹
venv\Scripts\pythonw.exe ha_server.py
```

`pythonw.exe` 是无窗口模式，服务在后台运行。如需查看日志，改用 `python.exe`：

```bash
venv\Scripts\python.exe ha_server.py
```

### 方式三：开机自启动

已配置注册表自启动后，每次开机登录 Windows 后，`start_service.vbs` 会自动检测并启动服务（静默后台运行，不弹窗口）。

---

## 九、服务常驻与浏览器控制

### 9.1 常驻后台

- `idle_shutdown_seconds: 0` 时，服务启动后一直常驻后台，即使关闭浏览器也不会退出
- 只有手动停止进程或关机才会终止
- 开机自启动确保每次开机后服务自动运行

### 9.2 浏览器控制开关

在配置页（http://127.0.0.1:8080/config.html）的 "服务管理" 卡片中：

- **充电服务运行状态**：开关控制充电服务的运行/停止
  - 开启：服务连接充电器，可控制端口、监控功率
  - 关闭：服务断开充电器连接，网页仍可访问但无法控制
- **开机自启动**：开关控制是否开机自动启动服务
- **每日定时重启**：设置每日自动重启时间（释放累积资源）

> 浏览器中的开关只影响当前运行的服务状态，不会终止 Web 服务进程。如需完全停止后台进程，可在任务管理器中结束 `pythonw.exe`。

### 9.3 端口状态自动管理

服务会自动管理端口的开关状态，确保使用连续性和安全性：

- **首次启动（开机自启 / 每日定时重启）**：服务连接充电器后，自动关闭所有端口（C1/C2/C3/USB-A），避免上电时端口意外输出
- **断联重连（充电器断电 / 跳闸后恢复）**：服务在 BLE 断联时保存各端口的开关状态，重连成功后自动发送指令恢复到断联前的状态，保证充电不中断
  - 适用于学校断电、跳闸、充电器重启等导致 PIID 16 寄存器被清空的场景
  - 恢复逻辑仅在服务进程未重启时生效；服务进程重启（开机/每日重启）则按"首次启动"处理，关闭所有端口

---

## 十、功能说明

### 10.1 端口控制

- 首页 "端口监控" 板块显示 C1/C2/C3/USB-A 四个端口
- 每个端口有独立开关，可单独控制通断
- 显示实时电压、电流、功率、充电协议
- 显示本次充电时长和倒计时

### 10.2 功率曲线

- 实时绘制各端口功率曲线
- 支持 1分/5分/10分/30分/1小时 时间范围
- 支持滚轮缩放、拖拽平移

### 10.3 充电时长限制

- 配置页可设置每个端口的最大充电时长（分钟）
- 达到上限自动断电
- 首页实时显示本次充电时长，剩余≤10分钟或已用80%变橙，达上限变红

### 10.4 倒计时

- 首页 "倒计时设置" 板块可为每个端口设置倒计时
- 倒计时结束自动断电
- 支持快捷预设（15/30/60/90/120/240分钟）

### 10.5 定时任务

- 首页 "定时控制" 板块可添加定时任务
- 支持指定端口、时间、动作（开启/关闭）
- 最多可添加 50 个定时任务
- 任务列表超出时可滚动查看

### 10.6 智能充电保护

- 仅 C1、C2 生效（C3/USB-A 不受影响）
- 单次连续充电中实时功率低于阈值并持续设定时长，自动断开
- 记录断开原因和时间

### 10.7 充电历史

- 自动记录每次充电会话（端口、开始/结束时间、时长、电量、协议）
- 支持按日筛选、导出 CSV
- 历史数据保留天数可配置（默认 365 天）

---

## 十一、常见问题

### Q1：服务启动后连不上充电器？

A：检查以下几点：
1. 电脑蓝牙是否开启
2. `config.yaml` 中的 MAC/Token/BLE Key 是否正确
3. 充电器是否通电
4. 查看日志：用 `python.exe ha_server.py` 启动查看错误信息

### Q2：小米云自动获取失败？

A：确保：
1. 小米账号已绑定酷态科充电器
2. 网络可访问小米云服务器
3. 登录时使用正确的小米账号密码
4. 如自动获取持续失败，尝试手动获取（见第六节方法二）

### Q3：巴法云远程控制不生效？

A：检查：
1. `bemfa.enabled: true`
2. `bemfa.uid` 私钥正确
3. 巴法云控制台设备名称与配置一致
4. 服务日志中是否有巴法云连接错误

### Q4：如何完全停止服务？

A：在任务管理器中找到 `pythonw.exe`（命令行含 `ha_server.py`），结束进程。或在配置页关闭 "充电服务运行状态"（仅断开充电器，Web 服务仍运行）。

### Q5：端口 8080 被占用？

A：修改 `config.yaml` 中 `server.port` 为其他端口（如 8081），然后重启服务。

### Q6：换电脑后如何迁移？

A：
1. 在新电脑上安装 Python 3.10+
2. 复制整个项目文件夹到新电脑（**不要复制 venv 文件夹**，需重新创建）
3. 复制 `config.yaml`（含你的 BLE/巴法云配置）
4. 运行 `install.bat` 重新安装
5. 启动服务即可

### Q7：服务每天凌晨自动重启？

A：这是 "每日定时重启" 功能（默认 06:30），用于释放累积资源。如需关闭，在配置页 "服务管理" 中关闭 "每日定时重启" 开关。

---

## 十二、项目文件结构

```
cuktech-ble-server/
├── ha_server.py          # 主服务入口（Web 服务 + BLE + MQTT）
├── ble_manager.py        # BLE 蓝牙管理
├── bemfa_client.py       # 巴法云客户端
├── charge_guard.py       # 智能充电保护
├── automation.py         # 定时任务管理
├── config.py             # 配置管理
├── state.py              # 状态管理
├── history.py            # 充电历史
├── xiaomi_cloud.py       # 小米云获取 BLE 信息
├── energy.py             # 能耗统计
├── downsample.py         # 功率曲线降采样
├── state_protocol_v2.py  # 状态协议
├── config.yaml           # 配置文件（需填入你的 BLE/巴法云信息）
├── config.yaml.example   # 配置模板
├── requirements.txt      # Python 依赖
├── install.bat           # 一键安装脚本
├── quick_start.vbs       # 快速启动器（桌面快捷方式用）
├── start_service.vbs     # 开机自启动脚本
├── 启动服务.bat           # 命令行启动脚本
├── README.md             # 项目说明
├── 部署指南.md            # 本文档
├── RELEASE_NOTES.md      # 版本更新日志
├── port_history.db       # 充电历史数据库（运行时生成）
├── web/                  # Web 前端
│   ├── index.html        # 首页
│   ├── config.html       # 配置页
│   ├── phone.html        # 移动端页面（本项目仅 PC 使用）
│   └── static/           # 静态资源（JS/CSS/图片/多语言）
└── venv/                 # Python 虚拟环境（install.bat 自动创建）
```

---

*文档版本：v1.0 | 更新日期：2026-09-02*
